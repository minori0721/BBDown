# BBDown for Bili-favorites-backup bfb-2.0.6

## 修复

- APP 请求恢复系统 TLS 证书验证，拒绝不受信任的服务端证书。
- 移除高风险调试日志中的认证参数、带签名的媒体地址、完整播放响应和请求正文；保留请求目标主机、状态码及响应长度等诊断信息。
- 收藏夹列表和分页响应检查接口状态；补齐默认收藏夹查询参数，并及时释放分页 JSON 文档。
- 章节接口失败会给出警告，下载继续进行但不生成章节；正常无章节的视频仍保持原行为。

## 范围与验证

- 这是 `bfb-2.0.5` 的补丁版本，不改变 BFB 集成协议、互动视频片段清单、命令行参数或常规下载流程。
- WSL2 Release 构建和命令行版本检查通过；单元测试 61 通过、0 失败。
- 使用自签名证书或未受系统信任的代理访问 APP 接口时，请先将证书正确加入系统信任链；本版不再跳过证书检查。
- Linux x64 Native AOT 产物由 GitHub Actions 构建，Release 附带 ZIP、SHA256 和来源提交信息。BFB 的固定版本与部署需另行更新。

不发布 NuGet 或 GitHub Packages，不改动历史 Release 资产。
